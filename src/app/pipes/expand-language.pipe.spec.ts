import { ExpandLanguagePipe } from './expand-language.pipe';

describe('ExpandLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new ExpandLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
