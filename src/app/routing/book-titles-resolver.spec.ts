import { BookTitlesResolver } from './book-titles-resolver';

describe('BookTitlesResolver', () => {
  it('should create an instance', () => {
    expect(new BookTitlesResolver()).toBeTruthy();
  });
});
